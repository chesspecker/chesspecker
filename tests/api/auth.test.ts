import handler from '@/api/user/index';

describe('api/auth', () => {
	describe('login', () => {
		it('should be true', () => {
			expect(true).toBe(true);
		});
	});
	describe('logout', () => {
		it('should be true', () => {
			expect(true).toBe(true);
		});
	});
	describe('callback', () => {
		it('should be true', () => {
			expect(true).toBe(true);
		});
	});
});
